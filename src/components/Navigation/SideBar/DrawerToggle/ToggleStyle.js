import styled from 'styled-components';

const ToggleStyle = styled.div`
    img {
        cursor: pointer;
        display: flex;
        padding: 0.5rem;
        width: 10%;
        &:hover {
            color: '#212348';
       }
    }
`;

export default ToggleStyle;
